/* 知识库页面通用样式 - 按照Markdown导入制作规范 */

/* 页面容器 */
.page {
  min-height: 100vh;
  background: #ffffff; /* 纯白背景 */
}

/* 页面标题 */
.page-title {
  font-size: 48rpx !important;
  font-weight: bold !important;
  color: #000000 !important;
  margin-top: 48rpx !important;
  margin-bottom: 40rpx !important;
  padding-left: 48rpx !important;
}

/* 章节标题 */
.section-title {
  font-size: 40rpx;
  font-weight: bold;
  color: #000000;
  margin-bottom: 32rpx;
  padding-left: 48rpx;
}

/* 子章节标题 */
.subsection-title {
  font-size: 32rpx;
  font-weight: bold;
  color: #000000;
  margin-bottom: 24rpx;
  padding-left: 48rpx;
}

/* 文本内容样式 */
.section-content, .subsection-content {
  font-size: 30rpx;
  line-height: 1.6;
  color: #333333;
  margin-bottom: 32rpx;
  padding: 0 48rpx;
  text-align: justify;
}

.section-content text, .subsection-content text {
  display: block; /* 确保text组件换行 */
}

/* 列表样式 */
.list-container {
  padding: 0 48rpx 48rpx 48rpx;
}

.list-item {
  font-size: 30rpx;
  line-height: 1.6;
  color: #333333;
  margin-bottom: 16rpx;
  position: relative;
  padding-left: 24rpx;
}

.list-item::before {
  content: "•";
  position: absolute;
  left: 0;
  color: #4845F7;
  font-weight: bold;
}

/* 表格容器 - 三层嵌套结构 */
.table-wrapper {
  margin-bottom: 48rpx;
  overflow-x: auto;
  overflow-y: visible;
  -webkit-overflow-scrolling: touch;
  /* 使用padding来控制边距，避免滚动时内容被裁剪 */
  padding-left: 48rpx;
  /* 移除 padding-right，改为使用伪元素撑开 */
  padding-top: 8rpx;
  padding-bottom: 25rpx;
  width: 100%;
  box-sizing: border-box;
  /* 隐藏滚动条但保持滚动功能 */
  scrollbar-width: none; /* Firefox */
  -ms-overflow-style: none; /* IE/Edge */
  /* 让伪元素生效 */
  display: flex; 
  flex-direction: row;
}

/* 隐藏滚动条 */
.table-wrapper::-webkit-scrollbar {
  height: 0;
  width: 0;
  display: none;
}

/* 表格主体 - 使用伪元素撑开右边距 */
.table-container {
  display: table;
  width: auto;
  min-width: 800rpx; /* 设置最小宽度确保触发滚动 */
  border-radius: 24rpx;
  overflow: hidden; /* 确保圆角生效 */
  box-shadow: 0 8rpx 24rpx rgba(0, 0, 0, 0.05);
  table-layout: auto;
  border-collapse: separate;
  border-spacing: 0;
  border: 1rpx solid #ecf0f1; /* 外边框 */
  /* 关键：使用 margin-right 无法在 overflow 容器中撑开空间，需要使用伪元素技巧或者嵌套容器 */
}

/* 这是一个经典的 CSS 滚动容器右侧 padding 丢失问题的解决方案 */
/* 在 table-container 后面添加一个不可见的伪元素或者 spacer */
.table-wrapper::after {
  content: "";
  display: block;
  min-width: 48rpx; /* 强制占据 48rpx 宽度 */
  height: 1px;
}

/* 表格头部 */
.table-header {
  display: table-row;
  background: linear-gradient(90deg, #A9FFCA 0%, #D2FFE3 100%);
  font-weight: bold;
}

.header-cell {
  display: table-cell;
  padding: 24rpx 48rpx;
  text-align: left;
  vertical-align: middle;
  border-right: 1rpx solid #ecf0f1;
  white-space: nowrap;
  font-size: 28rpx;
  color: #333333;
}

/* 表格数据行 */
.table-row {
  display: table-row;
  background-color: #ffffff;
  border-bottom: 1rpx solid #ecf0f1;
}

.table-row:last-child {
  border-bottom: none;
}

/* 表格单元格 */
.table-cell {
  display: table-cell;
  padding: 24rpx 48rpx;
  vertical-align: middle;
  text-align: left;
  border-right: 1rpx solid #ecf0f1;
  white-space: nowrap;
  font-size: 28rpx;
  color: #666666;
  min-width: 120rpx;
  max-width: 500rpx;
  line-height: 1.5;
}

/* 表头单元格 */
.header-cell {
  background: transparent !important;
  color: #000000 !important;
}

/* 表格行颜色交替 */
.table-row:nth-child(odd) {
  background-color: #ffffff;
}

.table-row:nth-child(even) {
  background-color: #FAFAFA;
}

/* 表格圆角处理 */
/* 第一行圆角 */
.table-row:first-child .table-cell:first-child {
  border-top-left-radius: 24rpx;
}

.table-row:first-child .table-cell:last-child {
  border-top-right-radius: 24rpx;
}

/* 最后一行圆角 */
.table-row:last-child .table-cell:first-child {
  border-bottom-left-radius: 24rpx;
}

.table-row:last-child .table-cell:last-child {
  border-bottom-right-radius: 24rpx;
}

/* 移除最后一列的右边框 */
.table-cell:last-child {
  border-right: none;
}

/* 错误状态 */
.error {
  text-align: center;
  color: #999;
  font-size: 28rpx;
  padding: 80rpx 48rpx;
}

/* 页面底部空白边距 */
.page::after {
  content: '';
  display: block;
  height: 80rpx;
}